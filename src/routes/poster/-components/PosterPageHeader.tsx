export function PosterPageHeader() {
  return (
    <div className="mb-6">
      <h1 className="text-3xl font-bold mb-2">Poster</h1>
      <p className="text-muted-foreground">
        Split an image across multiple printable pages, then glue them together
        into a poster. Adjust paper size, margins, overlap, and position in the
        preview, then download a multi-page PDF.
      </p>
    </div>
  )
}
