export function SpotlightPageHeader() {
  return (
    <div className="mb-6">
      <h1 className="text-3xl font-bold mb-2">Spotlight</h1>
      <p className="text-muted-foreground">
        Draw rectangles or ellipses, optionally add a magnifier inset zoom with
        connector lines. Darken or blur the rest, then export as JPG, PNG, or
        WEBP.
      </p>
    </div>
  )
}
